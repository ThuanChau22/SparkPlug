services:

  sql-db-service:
    image: sparkplug-station-analytics-service
    container_name: sparkplug-station-analytics-service
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - ${PORT}:${PORT}
